{
	"version": "2.0.0",
	"tasks": [
		{
			"type": "docker-compose",
			"label": "docker-compose: debug:paybill",
			"dockerCompose": {
				"up": {
					"detached": true,
					"services": ["paybill"]
				},
				"files": [
					"${workspaceFolder}/docker-compose.yaml",
					"${workspaceFolder}/docker-compose-debug.yaml"
				]
			}
		}
	]
}
